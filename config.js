module.exports = {
    // Job title or search keyword
    keyword: "Aerospace Engineer",

    // Location (postcode or city)
    location: "G82",

    // Maximum number of result pages to scrape
    maxPages: 3,

    // Output CSV path
    outputFile: "output/jobs.csv"
};
